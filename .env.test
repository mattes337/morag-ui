# Test Environment Configuration

# API Configuration
# Backend MoRAG service URL (server-side only)
MORAG_API_URL=http://localhost:8000
# Backend MoRAG service API key (optional, for authenticated access)
MORAG_API_KEY=
# Client-side API URL (must be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Database URL for testing (using development MySQL container)
DATABASE_URL="mysql://morag_user:morag_password@localhost:3307/morag_dev_db"

# JWT Secret for authentication
JWT_SECRET="test-secret-key"

# Test-specific settings
NODE_ENV=test
